export type rightDrawerType = {
    header: string;  // current  title state management
    isOpen?: boolean;   // right drawer state management for opening closing
    bodyType?: string;   // right drawer content management
    extraObject?: object;
}
